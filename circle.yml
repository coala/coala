dependencies:
  pre:
    - bash .misc/deps.pre.sh
  override:
    - bash .misc/deps.sh

test:
  override:
    - bash .misc/tests.sh:
        parallel: true
    - python setup.py install:
        parallel: true
    - coala-ci:
        parallel: true

deployment:
  deployment_scripts:
    branch: /.*/
    commands:
      - bash .misc/deploy.coverage.sh
      - bash .misc/deploy.zanata.sh
