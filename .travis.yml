language: python

notifications:
  recipients:
    - lasse.schuirmann@gmail.com
    - fabian@neuschmidt.de
  email:
    on_failure: always
    on_success: change

python:
  - "3.2"
  - "3.3"
  - "3.4"

script:
  - python ./execute_all_tests.py
# TODO make sure pot file is up to date (this seems to break travis sometimes (?))
#  - sh ./tests/potupdatetest.sh
# TODO test installation with a bash script
#  - sudo python setup.py install

